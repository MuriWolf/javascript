// calcular area

var base = Number(window.prompt("Digite o valor da base: "))
var altura = Number(window.prompt("Digite o valor da altura: "))

var area = base * altura

document.write(`O valor da area é de ${area} metros`)